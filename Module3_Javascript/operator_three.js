const userName ='Victory'
const age =12

/*
And =&& All must be true
OR = || either must be true
not =! toggles or changes boolean values
*/

//const logic = age === 12 && userName === 'Success'
const logic = age === 12 || userName === 'Success'


console.log(!logic)
console.log(logic)